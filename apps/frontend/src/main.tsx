import React from 'react'; import { createRoot } from 'react-dom/client'; import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import App from './App'; import { AuthPage, SessionGate } from './AuthPages'; import { AuthenticatedShell, InvitationAccept } from './AuthenticatedShell'; import './styles.css';
const client=new QueryClient(); const authPage=location.pathname==='/login'?<AuthPage mode="login"/>:location.pathname==='/register'?<AuthPage mode="register"/>:<SessionGate>{import.meta.env.VITE_AUTH_ENABLED==='true'?location.pathname.startsWith('/invitations/')?<InvitationAccept token={location.pathname.split('/')[2]}/>:<AuthenticatedShell/>:<App/>}</SessionGate>; createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={client}>{authPage}</QueryClientProvider></React.StrictMode>);
